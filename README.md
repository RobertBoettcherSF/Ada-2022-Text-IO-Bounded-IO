# Text_IO.Bounded_IO in Ada 2022

Teaching sheet for `Ada.Text_IO.Bounded_IO`. No SPARK. Wave 23.

Shows `Put_Line` / `Get_Line` of a `Bounded_String` through a temp file.

```bash
make && make test
```
